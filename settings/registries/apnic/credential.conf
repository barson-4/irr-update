##
## Credential / local values (NOT for human-input secrets)
##

# NOTE:
# - Put stable org/user settings here.
# - This file may contain sensitive information.
# - Set restrictive permissions (e.g., chmod 600).
#
# IRR_PASSWORD:
# - Used for IRR database authentication (mail body password field).
#
# CRYPT_PW:
# - Used for mntner object updates (CRYPT-PW).
# - This tool treats it as plaintext.
# - Hashing is handled by the IRR server when applicable.

IRR_PASSWORD=""
CRYPT_PW=""
